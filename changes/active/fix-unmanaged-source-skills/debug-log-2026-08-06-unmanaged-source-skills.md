> 来源: debugging skill
> 生成时间: 2026-08-06
> 阶段: debugging

# 排查记录：分类内 Skill 变更后失去受控状态

## 问题

工作流分类内删除或移动 Skill 后，旧软链接会成为断链；切换逻辑依赖 `realpath` 校验断链，因目标不存在而跳过清理，随后又覆盖状态文件，导致原受控链接永久降级成非受控项（逻辑问题）。

## 排查过程

- [x] 检查切换、状态和软链接校验代码 → 确认断链的 `realpath` 返回空值。
- [x] 检查 Codex 当前目录 → 确认 10 个警告项均为已配置 Personal source 下的失效旧链接。
- [x] 增加删除、移动测试 → 修复前稳定复现跳过清理和同名冲突。
- [x] 增加新增、原子预检测试 → 确认新增项写入状态，异常项不会造成部分删除。

## 修复

- **改了**：`src/switcher.mjs` 改为比较软链接原始目标与状态记录，并在目标不存在时按配置 source 路径边界校验；切换前统一预检全部旧受控项，通过后再删除。
- **测试**：`test/switcher.test.mjs` 新增分类内新增、删除、移动和预检原子性测试。
- **当前状态**：安全删除 10 个已确认断链；全局命令完成 `Personal → ZW → Personal` 双向切换，最终为 Personal。
- **理由**：保留状态记录作为所有权依据，同时不放宽对非符号链接、目标被篡改或越界路径的保护。
- **验证**：✅ `npm run check`、20/20 测试、`git diff --check` 全部通过。
- **审查**：✅ 六维度自检通过；修改仅涉及受控链接校验、对应测试和本排查记录。
